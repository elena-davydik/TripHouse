import styled from "styled-components";
import backgroundImg from "../../assets/backgrounds/hero-bg.png";

export const StyledSingIn = styled.div`
  height: 100vh;
  display: flex;
  flex-direction: column;

  background-image: url(${backgroundImg});
`;
